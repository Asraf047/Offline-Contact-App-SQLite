<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amanullah.myapplication18">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".database.ListActivity"
            android:label="listactivity"
            android:theme="@style/AppTheme2"/>
        <activity
            android:name=".database.ModifyActivity"
            android:label="listactivity"
            android:theme="@style/AppTheme2"/>
        <activity
            android:name=".database.AddActivity"
            android:label="listactivity"
            android:theme="@style/AppTheme2"/>
        <activity
            android:name=".database.ResultActivity"
            android:label="listactivity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".ListActivity1"
            android:label="listactivity" />
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Edit1"
            android:theme="@style/AppTheme2" >
        </activity>
    </application>

</manifest>